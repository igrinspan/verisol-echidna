pragma solidity >=0.4.24 <0.9.0;

contract CrowdfundingR {
	modifier hasInitialized {
		require(has_initialized); 
		_; 
	}

	modifier hasNotInitialized {
		require(!has_initialized); 
		_; 
	}

	bool has_initialized = false;

	function vc0x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && !((max_block < blockNumber && goal <= balance)) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && !(donadores_reentrada.length > 0) && !(donadores_reentrada.length == 0)));
	}
	function vc1x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && !((max_block < blockNumber && goal <= balance)) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && !(donadores_reentrada.length > 0) && !(donadores_reentrada.length == 0)));
	}
	function vc2x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && (max_block < blockNumber && goal <= balance) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && !(donadores_reentrada.length > 0) && !(donadores_reentrada.length == 0)));
	}
	function vc3x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && !((max_block < blockNumber && goal <= balance)) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && !(donadores_reentrada.length > 0) && !(donadores_reentrada.length == 0)));
	}
	function vc4x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && !((max_block < blockNumber && goal <= balance)) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && donadores_reentrada.length > 0 && !(donadores_reentrada.length == 0)));
	}
	function vc5x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && !((max_block < blockNumber && goal <= balance)) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && !(donadores_reentrada.length > 0) && donadores_reentrada.length == 0));
	}
	function vc6x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && (max_block < blockNumber && goal <= balance) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && !(donadores_reentrada.length > 0) && !(donadores_reentrada.length == 0)));
	}
	function vc7x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && !((max_block < blockNumber && goal <= balance)) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && !(donadores_reentrada.length > 0) && !(donadores_reentrada.length == 0)));
	}
	function vc8x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && !((max_block < blockNumber && goal <= balance)) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && donadores_reentrada.length > 0 && !(donadores_reentrada.length == 0)));
	}
	function vc9x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && !((max_block < blockNumber && goal <= balance)) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && !(donadores_reentrada.length > 0) && donadores_reentrada.length == 0));
	}
	function vc10x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && (max_block < blockNumber && goal <= balance) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && !(donadores_reentrada.length > 0) && !(donadores_reentrada.length == 0)));
	}
	function vc11x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && (max_block < blockNumber && goal <= balance) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && donadores_reentrada.length > 0 && !(donadores_reentrada.length == 0)));
	}
	function vc12x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && (max_block < blockNumber && goal <= balance) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && !(donadores_reentrada.length > 0) && donadores_reentrada.length == 0));
	}
	function vc13x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && !((max_block < blockNumber && goal <= balance)) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && donadores_reentrada.length > 0 && !(donadores_reentrada.length == 0)));
	}
	function vc14x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && !((max_block < blockNumber && goal <= balance)) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && !(donadores_reentrada.length > 0) && donadores_reentrada.length == 0));
	}
	function vc15x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && !((max_block < blockNumber && goal <= balance)) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && donadores_reentrada.length > 0 && donadores_reentrada.length == 0));
	}
	function vc16x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && (max_block < blockNumber && goal <= balance) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && !(donadores_reentrada.length > 0) && !(donadores_reentrada.length == 0)));
	}
	function vc17x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && (max_block < blockNumber && goal <= balance) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && donadores_reentrada.length > 0 && !(donadores_reentrada.length == 0)));
	}
	function vc18x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && (max_block < blockNumber && goal <= balance) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && !(donadores_reentrada.length > 0) && donadores_reentrada.length == 0));
	}
	function vc19x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && !((max_block < blockNumber && goal <= balance)) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && donadores_reentrada.length > 0 && !(donadores_reentrada.length == 0)));
	}
	function vc20x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && !((max_block < blockNumber && goal <= balance)) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && !(donadores_reentrada.length > 0) && donadores_reentrada.length == 0));
	}
	function vc21x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && !((max_block < blockNumber && goal <= balance)) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && donadores_reentrada.length > 0 && donadores_reentrada.length == 0));
	}
	function vc22x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && (max_block < blockNumber && goal <= balance) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && donadores_reentrada.length > 0 && !(donadores_reentrada.length == 0)));
	}
	function vc23x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && (max_block < blockNumber && goal <= balance) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && !(donadores_reentrada.length > 0) && donadores_reentrada.length == 0));
	}
	function vc24x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && (max_block < blockNumber && goal <= balance) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && donadores_reentrada.length > 0 && donadores_reentrada.length == 0));
	}
	function vc25x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && !((max_block < blockNumber && goal <= balance)) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && donadores_reentrada.length > 0 && donadores_reentrada.length == 0));
	}
	function vc26x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && (max_block < blockNumber && goal <= balance) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && donadores_reentrada.length > 0 && !(donadores_reentrada.length == 0)));
	}
	function vc27x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && (max_block < blockNumber && goal <= balance) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && !(donadores_reentrada.length > 0) && donadores_reentrada.length == 0));
	}
	function vc28x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && (max_block < blockNumber && goal <= balance) && !((blockNumber > max_block && !funded && goal > balance && _donadores > 0)) && donadores_reentrada.length > 0 && donadores_reentrada.length == 0));
	}
	function vc29x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && !((max_block < blockNumber && goal <= balance)) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && donadores_reentrada.length > 0 && donadores_reentrada.length == 0));
	}
	function vc30x0x0() hasInitialized payable public {
		assert(!(!((max_block > blockNumber)) && (max_block < blockNumber && goal <= balance) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && donadores_reentrada.length > 0 && donadores_reentrada.length == 0));
	}
	function vc31x0x0() hasInitialized payable public {
		assert(!((max_block > blockNumber) && (max_block < blockNumber && goal <= balance) && (blockNumber > max_block && !funded && goal > balance && _donadores > 0) && donadores_reentrada.length > 0 && donadores_reentrada.length == 0));
	}
    address payable owner;
    uint max_block;
    uint goal;
    uint blockNumber;
    
    mapping ( address => uint ) backers;
    // address[] backersArray = new address[](0);
    // address[] auxArray;
    bool funded = false;
    uint balance = 0;
    // uint _cont = 0;
    address _last;
    uint _donadores = 0;
    address[] donadores_reentrada = new address[](0);

    function my_constructor( address payable _owner , uint _max_block , uint _goal, uint _blockNumber ) hasNotInitialized public {
        owner = _owner;
        max_block = _max_block;
        goal = _goal;
        balance = 0;
        blockNumber = _blockNumber;
				has_initialized = true;
    }

}